# Bank Statement Extractor — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# File upload support (multipart/form-data)
python-multipart>=0.0.9

# Data validation & serialisation
pydantic>=2.7.0

# MinerU model via HuggingFace transformers
transformers>=4.47.0
torch>=2.2.0
torchvision>=0.17.0
accelerate>=0.30.0
Pillow>=10.0.0

# PDF → image conversion (requires poppler system library)
pdf2image>=1.17.0

# Async HTTP client (Tally ERP XML API)
httpx>=0.27.0
