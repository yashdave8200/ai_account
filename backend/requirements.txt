# Bank Statement Extractor — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# File upload support (multipart/form-data)
python-multipart>=0.0.9

# Data validation & serialisation
pydantic>=2.7.0

# OpenAI GPT-4o mini vision API
openai>=1.0.0

# Load environment variables from .env
python-dotenv>=1.0.0

# PDF → image conversion (requires poppler system library)
pdf2image>=1.17.0

# Async HTTP client (Tally ERP XML API)
httpx>=0.27.0
